/**
 * @fileOverview Default code to run on every page
 * @author <a href="mailto:leonardseymore@gmail.com">Leonard Seymore</a>
 */

"use strict";

$(document).ready(function() {
  $("input[type=submit], input[type=button], button").button();
});